import { Layout } from "react-admin";
//import { NotificationBell} from "./NotificationBell";

//export const MyLayout = (props: any) => (
 //   <Layout {...props} appBar={() => <NotificationBell />} />
//);
export const MyLayout = (props: any) => <Layout {...props} />;